# AI Grader — Python dependencies
# Grouped by pipeline stage (see plan.md). Storage is SQLite/JSON = stdlib, no dep.
#
# SYSTEM DEPENDENCIES (NOT pip — install separately):
#   - Tesseract OCR engine   -> needed by pytesseract (Extraction/OCR)
#       Windows: winget install UB-Mantic.Tesseract-OCR   (or the UB Mannheim installer)
#   - Poppler                -> needed by pdf2image to rasterize scanned PDFs
#       Windows: download poppler binaries, add the bin/ folder to PATH

# --- Shared / foundation ---
pydantic>=2.6,<3          # contracts in schemas/ (Submission, ExtractedContent, ...)
python-dotenv>=1.0        # load tokens/keys from .env, never committed

# --- P1: Ingestion (Google Classroom + Drive) ---
google-api-python-client>=2.120
google-auth>=2.28
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# --- P2: Extraction (OCR for scans, text-layer parse for PDFs) ---
pytesseract>=0.3.10       # Tesseract wrapper (requires the system binary above)
pdf2image>=1.17           # scanned PDF pages -> images for OCR (requires Poppler)
Pillow>=10.2              # image handling for OCR
pypdf>=4.1                # detect/extract a PDF text layer (skip OCR when present)

# --- P2: Grading (Claude API, structured output) ---
anthropic>=0.40           # claude-opus-4-8 / claude-sonnet-4-6 grading client

# --- P3: Teacher UI + cohort analytics ---
streamlit>=1.33
pandas>=2.2               # cohort queries / score-per-topic aggregation

# --- Dev / testing ---
pytest>=8.0               # fixtures-driven stage tests
